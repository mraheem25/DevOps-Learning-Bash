# Standard Environment Variables

| **Variable** |                           **Explanation**                          |
| ------------ | -------------------------------------------------------------------|
| `LOGNAME`    | Represents login name of current user                              |
| `SHELL`      | Stores the path of the current user's shell (e.g. `/bin/zsh`)      |
| `PWD`        | **P**rints the current **W**orking **D**irectory                   |
| `PATH`       | Shows the system executables search path and the binary locations  |
| `LANG`       | The system’s default language settings                             |
